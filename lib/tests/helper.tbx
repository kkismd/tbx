# lib/tests/helper.tbx — test support words for TBX library tests

DEF ASSERT(COND)
  IF COND
    RETURN
  ENDIF
  ASSERT_FAIL
END

DEF ASSERT_MSG(COND, MSG)
  IF COND
    RETURN
  ENDIF
  ASSERT_FAIL_MSG MSG
END
